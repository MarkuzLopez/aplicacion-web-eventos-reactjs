import  React from 'react'; 

const Header = () => { 
    return (
        <header className="ukmargin" uk-margin="true">
            <h1 className="uk-text-center">Eventos</h1>
        </header>
    );
}

export default Header;